<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.announcement.dao.AnnoSearchDAO">
	<select id="selectRegionList" resultType="hashmap">
		SELECT REGION_CODE "regionCode", REGION_NAME "regionName", REGION_REF "regionRef" 
		FROM CD_REGION
		<where>
			<if test="@org.apache.commons.lang3.StringUtils@isNotBlank(regionRef)">
				WHERE REGION_REF = #{regionRef}
			</if>
		</where>
		ORDER BY 1
	</select>
	<select id="selectIndustry" resultType="hashmap">
		SELECT INDUSTRY_CODE "industryCode", INDUSTRY_NAME "industryName", INDUSTRY_REF "industryRef" 
		FROM CD_INDUSTRY
		<where>
			<if test="@org.apache.commons.lang3.StringUtils@isNotBlank(industryRef)">
				WHERE INDUSTRY_REF = #{industryRef}
			</if>
		</where>
		ORDER BY 1
	</select>
	<select id="selectJob" resultType="hashmap">
		SELECT JOB_CODE "jobCode", JOB_NAME "jobName", JOB_REF "jobRef" 
		FROM CD_JOB
		<where>
			<if test="@org.apache.commons.lang3.StringUtils@isNotBlank(jobRef)">
				WHERE JOB_REF = #{jobRef}
			</if>
		</where>
		ORDER BY 1
	</select>
</mapper>